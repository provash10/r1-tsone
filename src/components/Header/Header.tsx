import React from 'react';

const Header = () => {
    return (
        <header className="text-2xl font-bold text-green-500 italic underline">TS 
          <span className="text-yellow-500">One</span>
        </header>
    );
};

export default Header;